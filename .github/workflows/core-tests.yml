name: Core tests
on:
  push:
  pull_request:
jobs:
  deterministic-core:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - run: dotnet test dev/TwoBrains.Tests/TwoBrains.Tests.csproj
